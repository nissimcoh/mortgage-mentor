/**
 * Internal calculation/schema audit version — stamped on every saved
 * scenario so a future methodology change can tell which engine version
 * produced a given result_snapshot. Bump only when calculation
 * methodology changes, not on every unrelated app deploy.
 */
export const CALCULATOR_VERSION = "1.0.0";
